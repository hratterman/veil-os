[package]
name = "hello-veil"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
veil-sdk = { path = "../../veil-sdk" }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
